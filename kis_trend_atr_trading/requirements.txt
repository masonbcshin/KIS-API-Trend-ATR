# KIS Trend-ATR Trading System Dependencies
# Python 3.8+ 권장

# HTTP 요청 라이브러리
requests>=2.31.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# 환경변수 관리
python-dotenv>=1.0.0

# YAML 설정 파일 파싱
PyYAML>=6.0.0

# 기술적 지표 계산
ta>=0.10.2

# PostgreSQL 데이터베이스
# psycopg2-binary는 빌드 없이 설치 가능한 버전
# 프로덕션에서는 psycopg2 (빌드 필요) 권장
psycopg2-binary>=2.9.9

# 테스트
pytest>=8.0.0
pytest-cov>=4.0.0
